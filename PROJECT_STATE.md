# PROJECT_STATE

## 当前阶段
`miniprogram-phase1` 已通过 UI 收口（Phase 2）。三个页面的视觉体验已升级：首页更像产品总控台、单日计划表单更清晰、今日作战图时间轴更易读。H5 Alpha v0.2 继续作为稳定基线。

## 本轮已完成
- **小程序 UI 收口完成**（2026-05-13）：
  - **首页**：英雄区新增装饰条，作战卡增加层级阴影，功能入口改为图标+文本+箭头三栏布局，随后事件改为独立卡片。
  - **单日计划**：各区块标题带分隔线，按钮添加过渡动画，输入框补上 placeholder 引导。
  - **今日作战图**：时间轴改为带竖线连接线 + 彩色圆点指示器（6 种事件类型各有着色），摘要卡加入分类图标，空状态改为居中视觉。
- **不变**：所有页面功能逻辑、数据绑定、事件处理完全保持 Phase 1 状态，只改展示层。
- **H5 Alpha v0.2 不受影响**：核心算法、规则引擎、localStorage key 均未修改。

## 当前风险
- Phase 1/2 补剂事件仍是简化版，尚未迁移完整补剂规则引擎。
- 小程序仍只支持 `singleDayPlan`，周计划、宏量预算、补剂完整页和长期计划未迁移。

## 下一步建议
- 下一轮建议：决定 Phase 3 方向。
  - 保持小程序现状，继续找人试用收集反馈
  - 还是补剂功能完善
  - 或迁移周计划支持

## 当前阶段
H5 Alpha v0.2 已完成稳定收口，并开始进入微信小程序迁移评估阶段。本轮没有正式迁移业务功能，只完成了迁移方案文档和最小小程序骨架，用于判断是否启动 Phase 1。

## 本轮已完成
- 新增 `MINIPROGRAM_MIGRATION_PLAN.md`，明确 H5 架构、小程序目标页面、存储替换、可复用逻辑、web-view 风险、原生分阶段路线和 MVP 范围。
- 新增 `miniprogram/` 基础骨架：
  - `app.json`
  - `app.js`
  - `app.wxss`
  - `pages/index/index.wxml`
  - `pages/index/index.wxss`
  - `pages/index/index.js`
- 骨架首页仅包含总控台验证版标题和三个入口占位，不承载正式业务逻辑。
- H5 版本、核心算法、localStorage key、规则引擎均未修改。

## 当前风险
- 小程序正式迁移时，`app_state.js` 需要做 `localStorage` 到 `wx` storage 的适配层，不能直接照搬。
- `daily_flow_engine.js`、`macro_rule_engine.js`、`supplement_rule_engine.js` 可复用价值高，但仍要检查浏览器 API 依赖。
- web-view 虽然快，但受业务域名、备案、体验和审核约束，不建议作为正式主方案。

## 下一步建议
- 下一轮先决策是否启动小程序 Phase 1。
- 如果启动 Phase 1，建议只做总控台 + 轻量单日计划 + 今日作战图，并优先迁移本地存储适配层和 `daily_flow_engine.js`。

## 当前阶段
daily_flow_engine 收口版 Alpha 已完成线上验收。线上地址 `https://charming-baklava-9ecd7b.netlify.app/` 中，首页“下一步作战提示”和模块四“今日作战图”已经确认共用 `daily_flow_engine.js` 事件生成逻辑，主链路表现一致。

## 本轮已完成
- 完成线上资源检查：`index.html`、`daily_flow_engine.js`、`app_state.js`、`app_ui.css`、`supplement_rules.json`、`macro_algorithm_rules.json` 均返回 200。
- 完成首页验收：无数据状态、快速体验状态、下一步事件渲染均正常。
- 完成模块四验收：今日摘要、完整时间流、训练来源与折叠详情结构正常。
- 完成一致性检查：首页下一步事件可以在模块四时间流中对应找到，两者来自同一个 `DailyFlowEngine` 事件数组。
- 完成 `single_day` 跨天检查：`21:00` 训练、`00:30` 入睡时，入睡事件显示为 `次日 00:30`，且未排在时间轴第一项。
- 完成 `weekly` 检查：`30 秒快速体验` 后模块四仍显示“本周训练计划”，不受 `singleDayPlan` 干扰。
- 完成晚间咖啡因检查：晚间咖啡因阻断仍生效。

## 当前风险
- 事件生成已经统一，但模块四的详情展示仍比首页丰富；这属于展示层差异，不再是事件来源差异。
- 后续如果扩展更多事件类型，应优先在 `daily_flow_engine.js` 增加事件，再让首页和模块四分别读取同一事件数组。

## 下一步建议
- 下一轮先做产品优先级判断：继续体验打磨，或恢复长期计划功能开发。
- 如果试用者仍反馈“结果页信息多”，优先继续打磨模块四展示密度；如果反馈已经稳定，则可以恢复长期计划功能开发。

## 当前阶段
工程收口版 Alpha 已完成本地实现：首页“下一步作战提示”和模块四“今日作战图”现在共用 `daily_flow_engine.js` 生成事件数组。当前主要目标已从“修功能和收 UI”进入“统一事件出口，减少后续规则扩展时的展示偏差”。

## 本轮已完成
- 新增 `daily_flow_engine.js`，统一跨天时间、事件排序、事件结构和下一步提取逻辑。
- 首页改为从 `AppState.getTodayDashboardContext()` 读取 context，再通过 `DailyFlowEngine` 渲染下一步卡片。
- 模块四改为从 `DailyFlowEngine` 读取完整事件数组，再按既有减负版结构渲染时间流。
- `single_day / weekly / long_term` 模式训练来源保持不变。
- 晚间咖啡因阻断、跨天睡眠显示、缺数据引导都保持不变。
- 发布包已纳入 `daily_flow_engine.js`，待本轮重新部署。

## 当前风险
- 首页与模块四现在已经共用同一事件源，但模块四的补剂“可选建议”详情仍来自页面内已有的辅助逻辑，而不是公共引擎；这不会影响首页/模块四的主时间流一致性，但后续如果要继续收口，仍可再评估是否把详情辅助数据也统一出去。
- 当前项目仍是“单文件页面 + 本地脚本”的 H5 结构，编码和历史兼容字段较多，后续继续迭代时需要持续避免大规模重构。

## 下一步建议
- 先重新部署 `daily_flow_engine` 收口版 Alpha。
- 线上重点复验：首页下一步、模块四完整时间流、跨天睡眠、晚间咖啡因阻断和发布包文件完整性。

## 当前阶段
模块四减负版 Alpha 已完成本地实现与验收。当前首页吸引力问题已基本缓解，真实试用反馈明确集中在模块四“今日作战图”的信息密度，因此本轮继续只压缩模块四首屏展示层，不动算法和数据结构。

## 本轮已完成
- 强化模块四首屏「今日摘要」，让训练、营养、补剂和关键提醒优先呈现。
- 单日时间流默认只展示关键节点，详细来源、剂量和说明继续保留在折叠详情中。
- `训练详情 / 营养详情 / 补剂详情` 三个区块默认收起，减少首屏压力。
- 无营养预算时，营养详情内新增跳转到模块二的按钮。
- 保持 `single_day / weekly / long_term` 训练来源逻辑、跨天睡眠显示、晚间咖啡因阻断与缺数据引导不变。
- 本地浏览器验收通过，`alpha-release` 待本轮重新构建。

## 当前风险
- 模块四虽然已经进一步减负，但它仍然是全站信息最密集的页面；当时间流事件很多时，仍可能继续收到“信息偏多”的反馈。
- 首页“下一步作战提示”和模块四时间流目前仍是同一数据链路上的轻量汇总与完整展示，并非完全同一个事件渲染器；后续如果规则继续复杂化，仍需继续观察两边是否出现展示偏差。

## 下一步建议
- 先重新部署模块四减负版 Alpha 并做真实试用者复测。
- 如果复测反馈仍聚焦“模块四信息多”，下一轮继续只压展示层；如果反馈开始转向首页与模块四不一致，再评估统一两边事件生成逻辑。

## 当前阶段
首页“下一步作战提示”版 Alpha 已完成线上功能验收。当前首页已经具备更强的首屏吸引力和行动提示能力，但“是否真的更抓眼、更像产品”这类主观结论，仍需要至少 1 位真实试用者复测后确认。

## 本轮已完成
- 完成线上地址 `https://elegant-naiad-ff3cb2.netlify.app/` 的首页与主链路验收。
- 确认以下资源线上返回 200：
  - `index.html`
  - `app_state.js`
  - `app_ui.css`
  - `单日计划_轻量模式.html`
  - `模块四_单日仪表盘.html`
- 确认首页大卡片线上可用：
  - 显示“现在该做什么？”
  - 当前时间正常
  - 无数据时引导正常
  - 示例用户状态下下一步事件正常
  - 单日跨天状态下“次日 00:30”显示正常
- 确认模块四仍正常：
  - 时间流显示正常
  - 首页“查看完整今日作战图”跳转正常
- 确认浏览器层面无明显问题：
  - 移动端首页卡片无横向溢出
  - 无 `SyntaxError`
  - 无 `ReferenceError`
  - 无资源 404

## 当前风险
- 目前首页作战卡与模块四时间流仍不是同一份事件生成器，只是基于同一数据链路做轻量汇总；后续如果规则继续扩展，两边仍有可能出现展示差异。
- 本轮缺少新的真实试用者主观反馈，因此“首页吸引力是否已经明显提升”仍未最终闭环。

## 下一步建议
- 下一轮优先补齐至少 1 位真实试用者首页复测。
- 复测时重点判断：
  - 首页是否更抓眼
  - 下一步作战提示是否真的有用
  - 首页与模块四是否存在让用户困惑的差异

## 当前阶段
首页总控台已经从“模式入口页”进一步升级为“模式入口 + 当前作战提示”首页。现在用户在进入三种模式之前，就能先看到当前时间、下一步该做什么，以及随后一步是什么，首页吸引力和行动感都更强。

## 本轮已完成
- 新增首页大卡片“现在该做什么？”。
- `AppState` 已提供 `getTodayNextAction(now)`，统一服务首页卡片。
- 首页下一步卡片已支持：
  - 当前时间
  - 当前作战阶段
  - 下一步事件
  - 倒计时
  - 随后一条事件
- 无数据时首页会直接引导：
  - `30 秒快速体验`
  - `开始单日计划`
- 跨天逻辑已复用现有作战日规则：
  - `00:30` 会显示为“次日 00:30”
  - 不会被排到当日最前
- 当前时间与倒计时改为每 30 秒自动刷新一次。
- 本地验收通过：
  - 无数据状态
  - 示例用户状态
  - 单日跨天 mock `23:40`
  - 移动端 `390 x 844`
  - 桌面端 `1280 x 800`
  - `alpha-release` 构建与本地打开

## 当前风险
- 首页“下一步作战提示”目前是对模块四时间流的轻量近似复用，而不是直接共享模块四内部渲染器；整体方向正确，但后续如果模块四事件规则继续变复杂，仍要继续观察两边是否会出现细微偏差。
- 当前卡片已经更有吸引力，但真正的“抓眼球”效果还需要试用者复测确认。

## 下一步建议
- 下一轮优先做线上部署与试用者复测，重点确认：首页是否更有吸引力、无数据引导是否更顺、跨天提示是否直观。

## 当前阶段
模块四减负版 Alpha 已完成本地实现和构建验收。当前全站的主要收口工作已经从“整体风格统一”进一步缩小到“最终结果页的信息层级优化”，即让今日作战图先显示今日摘要，再按需展开训练、营养和补剂细节。

## 本轮已完成
- 模块四首屏改为“今日摘要 + 单日时间流 + 折叠详情”结构。
- 今日摘要已稳定显示：
  - 今日训练
  - 今日营养
  - 今日补剂
  - 关键提醒
- 时间流默认展示减负完成：
  - 默认只显示时间、标题、简短说明和关键标签
  - 详细剂量、来源和补充说明改为条目内展开
- 训练详情 / 营养详情 / 补剂详情已做成默认收起的折叠区。
- 晚间咖啡因阻断、跨天入睡显示、single_day / weekly 训练来源逻辑均保持不变。
- 本地浏览器验收通过：
  - single_day 模式通过
  - weekly 模式通过
  - 缺数据状态通过
  - 移动端与桌面端布局通过
  - alpha-release 构建通过

## 当前风险
- 模块四虽然已经减负，但它仍然是全站信息最密集的页面；时间流一旦事件较多，用户仍可能继续感知到“信息偏多”。
- 训练详情中的 INOL 在轻量单日模式下可能仍为空值，这是符合当前数据链路的，但试用者可能会继续希望看到更完整的训练负荷说明。

## 下一步建议
- 下一轮优先做线上重部署与试用者复测，重点确认模块四信息密度是否真的下降。
- 如果复测反馈仍觉得模块四“第一屏信息多”，后续可以继续只减展示层，不动算法和数据结构。

## 当前阶段
UI 收口版 Alpha 已完成线上验收与复测记录。当前产品的三种模式入口、单日计划链路、补剂页定位、模块四跨天时间轴和全站视觉统一性都已达到可继续试用的状态。

## 本轮已完成
- 完成线上地址 `https://charming-baklava-9ecd7b.netlify.app/` 的资源与页面验收。
- 确认以下资源线上返回 200：
  - `index.html`
  - `app_ui.css`
  - `app_state.js`
  - `supplement_rules.json`
  - `macro_algorithm_rules.json`
  - `单日计划_轻量模式.html`
  - 模块一到模块五页面
- 完成移动端与桌面端线上 UI 审查：
  - 移动端导航未挤成多行
  - 页面无横向溢出
  - 首页首屏布局正常
  - 补剂页按钮和折叠区显示正常
  - 模块五 4 周卡片显示正常
  - 模块四时间流显示正常
- 完成线上功能冒烟：
  - `30 秒快速体验` 可用
  - `localStorage.clear()` 可用
  - 单日计划可保存并跳转模块四
  - 模块四显示 `训练来源：轻量单日计划`
  - `21:00` 训练与 `次日 00:30` 入睡显示正确
- 记录了试用者复测结论：
  - 页面整体更统一
  - 移动端拥挤感下降
  - 首页更像总控台
  - 单日计划足够轻量
  - 模块四更清晰，但信息密度仍偏高

## 当前风险
- 视觉复杂度整体已经下降，但模块四作为最终结果页，时间流和提示信息仍然最容易引发“信息太多”的感受。
- 长期计划页面虽然已纳入统一风格，但它依旧属于实验功能，当前不适合马上扩大功能范围。

## 下一步建议
- 下一轮优先判断：继续减负模块四信息密度，还是恢复长期计划功能开发。
- 结合本轮复测反馈，当前更推荐先继续减负模块四，再考虑恢复长期计划功能开发。
